import { Router } from 'express';
// import { getAll } from '../controllers/capsules';

const capsulesRouter = Router();

capsulesRouter.post('/create')
// capsulesRouter.get('/getAll' , getAll);

export default capsulesRouter;
